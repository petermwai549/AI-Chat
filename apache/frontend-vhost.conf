<VirtualHost *:80>
    ServerName frontend.local

    ProxyPreserveHost On

    RewriteEngine on
    RewriteCond %{HTTP:Upgrade} =websocket [NC]
    RewriteRule /(.*) ws://127.0.0.1:5270/$1 [P,L]

    ProxyPass / http://127.0.0.1:5270/
    ProxyPassReverse / http://127.0.0.1:5270/
</VirtualHost>
